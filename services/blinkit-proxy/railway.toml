# Deploy from this folder on Railway (Dockerfile).
# Set env: BLINKIT_PROXY_SECRET
# Public URL → Netlify BLINKIT_PROXY_URL
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
